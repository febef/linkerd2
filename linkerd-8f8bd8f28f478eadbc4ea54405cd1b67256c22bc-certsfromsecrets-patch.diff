diff --git a/charts/linkerd-control-plane/templates/identity.yaml b/charts/linkerd-control-plane/templates/identity.yaml
index ae375f200..8e4d22c7b 100644
--- a/charts/linkerd-control-plane/templates/identity.yaml
+++ b/charts/linkerd-control-plane/templates/identity.yaml
@@ -202,8 +202,8 @@ spec:
         volumeMounts:
         - mountPath: /var/run/linkerd/identity/issuer
           name: identity-issuer
-        - mountPath: /var/run/linkerd/identity/trust-roots/
-          name: trust-roots
+        - name: trust-roots
+          mountPath: /var/run/linkerd/identity/trust-roots
       {{- if not (empty .Values.identityProxyResources) }}
       {{- $r := merge .Values.identityProxyResources .Values.proxy.resources }}
       {{- $_ := set $tree.Values.proxy "resources" $r }}
@@ -245,9 +245,12 @@ spec:
       - name: identity-issuer
         secret:
           secretName: linkerd-identity-issuer
-      - configMap:
-          name: linkerd-identity-trust-roots
-        name: trust-roots
+      - name: trust-roots
+        secret:
+          secretName: linkerd-trust-anchor-roots
+          items:
+            - key: ca.crt
+              path: ca-bundle.crt
       {{ if not .Values.cniEnabled -}}
       - {{- include "partials.proxyInit.volumes.xtables" . | indent 8 | trimPrefix (repeat 7 " ") }}
       {{ end -}}
diff --git a/charts/linkerd-control-plane/templates/proxy-injector.yaml b/charts/linkerd-control-plane/templates/proxy-injector.yaml
index 8837af9bd..c5f74c62c 100644
--- a/charts/linkerd-control-plane/templates/proxy-injector.yaml
+++ b/charts/linkerd-control-plane/templates/proxy-injector.yaml
@@ -117,8 +117,8 @@ spec:
         volumeMounts:
         - mountPath: /var/run/linkerd/config
           name: config
-        - mountPath: /var/run/linkerd/identity/trust-roots
-          name: trust-roots
+        - name: trust-roots
+          mountPath: /var/run/linkerd/identity/trust-roots
         - mountPath: /var/run/linkerd/tls
           name: tls
           readOnly: true
@@ -150,8 +150,11 @@ spec:
       - configMap:
           name: linkerd-config
         name: config
-      - configMap:
-          name: linkerd-identity-trust-roots
+      - secret:
+          secretName: linkerd-trust-anchor-roots
+          items:
+            - key: ca.crt
+              path: ca-bundle.crt
         name: trust-roots
       - name: tls
         secret:
diff --git a/charts/partials/templates/_proxy.tpl b/charts/partials/templates/_proxy.tpl
index c28f9f03d..239429bfc 100644
--- a/charts/partials/templates/_proxy.tpl
+++ b/charts/partials/templates/_proxy.tpl
@@ -129,9 +129,9 @@ be used in other contexts.
 */}}
 {{- if .Values.proxy.loadTrustBundleFromConfigMap }}
   valueFrom:
-    configMapKeyRef:
-      name: linkerd-identity-trust-roots
-      key: ca-bundle.crt
+    secretKeyRef:
+      key: ca.crt
+      name: linkerd-trust-anchor-roots
 {{ else }}
   value: |
     {{- required "Please provide the identity trust anchors" .Values.identityTrustAnchorsPEM | trim | nindent 4 }}
